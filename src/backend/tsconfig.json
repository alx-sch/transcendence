{
  "files": [], // Tell TypeScript: "There is no code in this root folder."
  "references": [
    // Tell TypeScript: "Here are the sub-projects you need to build."
    { "path": "./utils" },
    { "path": "./user-service" }
  ]
}
